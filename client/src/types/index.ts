export interface InputBox{
    label: string
    type: string
    name: string
    placeholder: string
    textSize: string
}

export interface QuestionType{
    id: number
    label: string
    icon: string
}